# Build output
/server
*.exe
*.test

# Environment files (never commit secrets; .env.example is tracked)
.env
.env.*
!.env.example

# Local test/debug artifacts
cookies.txt
*.local

# Admin-uploaded product images (local filesystem storage backend).
# Never commit uploaded binary content; see UPLOAD_DIR in .env.example.
/data/uploads/

# Editor/OS
.DS_Store
